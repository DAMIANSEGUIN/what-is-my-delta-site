# Delta Troubleshooting Packet (copy-paste this to Claude/Gemini)

## Context
- Static single-page app (index.html + /assets + /data), hosted on Vercel.
- Preview deploy works; **Production returns 401/404**.
- Optional widgets (Voiceflow/Tidio/Landbot/Botpress).
- Optional Supabase DB; if unset, app runs local-only with LocalStorage.

## Symptoms
- URL failing: https://<prod-domain>/ (and/or /api/*)
- HTTP code: 401 or 404
- First seen: <YYYY-MM-DD HH:MM TZ>
- Preview URL works: <yes/no>
- Any recent changes: <list>

## Files / Settings (minimal surface area)
### vercel.json
<PASTE vercel.json HERE>

### CSP from index.html
<PASTE the meta Content-Security-Policy here>

### Error body/headers (copy from browser/network tab or curl)
<PASTE 3–10 lines of the response and key headers>

## Constraints
- Do NOT change framework or file layout.
- Prefer single-setting toggles or one-line header/meta changes.
- Provide a 60-second rollback.

## Ask
1) Root-cause hypothesis.
2) Lowest-risk fix (exact setting/file/line).
3) One-line test to confirm the hypothesis BEFORE changing code (curl/URL).
4) 60-second rollback.
